---
spring:
  profiles: cloud
management:
  port: -1

server:
  port: ${PORT:8989}

turbine:
  aggregator:
  appConfig: catalog-service,inventory-service
  InstanceMonitor:
    eventStream:
      skipLineLogic: false
